# Declaracion de tupla
tupla = (13, 1, 8, 3, 2, 5, 8)
# Creacion array list
list = []

# Ciclo for que recorra tupla
for tupla in tupla:
  # Condicional para imprimir los valores menores a 5
    if tupla < 5:
        list.append(tupla)
# Impresion de lista Final
        print(list)
